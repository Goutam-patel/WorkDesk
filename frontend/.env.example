# Client-side API proxy (always use /api for local and prod)
NEXT_PUBLIC_API_BASE_URL=/api

# Server-side backend URL (for Next.js rewrites)
BACKEND_URL=http://localhost:5001

# App name
NEXT_PUBLIC_APP_NAME=WorkDesk
